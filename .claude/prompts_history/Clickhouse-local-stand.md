Add a different docker-compose scenario with clickhouse only being deployed. It should be simple distributed clickhouse database.

For init.sql - create 2 test different schemas and 3 different random tables in each of them. Add some indexes onto some tables. Also create a random function in one of the schemas

